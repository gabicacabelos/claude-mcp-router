# Core
mcp>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0

# Opcional — ranking semántico local (si no está, se usa BM25 puro-Python):
# Nota: requiere onnxruntime; verificar compatibilidad con tu versión de Python.
# pip install fastembed
